# OR-Path 1.0 Closeout

**Date:** 2026-07-29  
**Status:** PASS (local gates)

## Scope closed
| Track | Evidence |
|-------|----------|
| Paper protocol P0–P3 | `scripts/paper_1_0_gate.py` → PAPER_1_0_PASS |
| Multi-agent M1–M3 | `scripts/subagent_gate.py` → M1/M2/M3_PASS |
| Anti-cosplay harness | lead no write/edit; must `subagent` toolCall (`docs/anti-cosplay-harness.md`) |
| Post-solve paper bridge | `orpath/post_solve_paper.py` + `orpath.bat paper-tube` |
| Tube-cut B2026 solve path | `scripts/b_tube_*.py`, PCA axial, BFD pack, result xlsx helpers |

## Runtime law (1.0)
1. Numbers only from solve tools + validate (never LLM optima).
2. LG owns stage machine; Pi is in-node lead for forced stages.
3. Forced stages (research/model/cite/review): real Pi `subagent` or hard fail when live.
4. Draft synthesis may be lead/script-owned; cited/review must be child-written under harness.
5. Deterministic gates force `ORPATH_LIVE_SUBAGENT=0`.

## Enable live multi-agent
```bat
set ORPATH_LIVE_SUBAGENT=1
orpath.bat run --live-subagent --fresh ...
orpath.bat paper-tube
orpath.bat subagent-gate
```

## Not claimed in 1.0
- Nightly CI always-on live Pi (needs key + long timeouts)
- Tube-cut proven OPTIMAL (labeled FEASIBLE / BFD)
- OpenPi GUI screenshot polish (optional)
