# OR-Path 1.1 Closeout — Problem Intake（题面 OCR + 自主审读）

**Date:** 2026-07-30  
**Status:** **PASS**  
**Law:** `specs/problem-intake.md`  
**Smoke ops:** `docs/1.1-smoke.md`

---

## Scope closed

| Track | Evidence |
|-------|----------|
| Spec freeze | `specs/problem-intake.md` + gates/product-scope/control-plane 挂接 |
| S1 gate 契约 | `tools/gate_intake.py` + fixtures 正/负例 |
| S2 OCR | `manual_stub` + `pdf_text`（pypdf）；图 OCR 占位 |
| S3 parse | 确定性 brief + `intake.json` |
| S4 DoD §9.2 | `scripts/intake_gate.py` 勾选清单 + skip_intake + Q1–Q4 结构 |
| S5 LG 前门 | `START→intake_ocr→intake_parse?→orchestrate`；默认 **skip** |
| S6 真题 smoke | **圆管 B2026** intake 竖切（本文件 + archive evidence） |

---

## Real-problem smoke（圆管 B 题）

**题：** 2026 数模校赛 B 题 — 异形圆管下料（与既有 `notes/b-tube-cut-2026-OCR-BRIEF.md` 同源）。

**输入：**

- 题面源（复现用）：`fixtures/intake/tube_cut_b2026/problem-surface.md`  
  （由 OCR brief 固化；**小文本**，可进 git；**不是**重解）
- 附件根：`fixtures/t3/tube_cut_b2026/raw/`（扫入 data_assets）

**命令（已跑）：**

```bat
set PYTHONNOUSERSITE=1
.venv-314\Scripts\python.exe orpath\run_orpath.py intake ^
  --slug b-tube-cut-1.1 ^
  --in fixtures\intake\tube_cut_b2026\problem-surface.md ^
  --assets fixtures\t3\tube_cut_b2026\raw

.venv-314\Scripts\python.exe tools\gate_intake.py ^
  outputs\b-tube-cut-1.1-intake.json ^
  --brief notes\b-tube-cut-1.1-problem-brief.md ^
  --min-subproblems 4
```

**结果：**

| 项 | 值 |
|----|-----|
| CLI | `ok: true`，`subproblems: 4` |
| `gate_intake` | **PASS** |
| 子问 | **Q1, Q2, Q3, Q4** |
| data_assets | **27**（附件树登记） |
| 禁键 | 无顶层 objective/path/tour/routes |
| ocr_backend | `manual_stub`（题面已是可读 md brief） |

**制品：**

| Path |
|------|
| `outputs/b-tube-cut-1.1-intake.json` |
| `notes/b-tube-cut-1.1-problem-brief.md` |
| `notes/b-tube-cut-1.1-ocr.raw.md` |
| `notes/b-tube-cut-1.1-ocr.meta.json` |
| `outputs/b-tube-cut-1.1-intake-cli.json` |
| `docs/archive/evidence/1.1-tube-cut-intake-smoke.json` |

---

## Gates

| Gate | Result | Note |
|------|--------|------|
| `scripts/intake_gate.py` | PASS | S6 日跑 FAST 模式勾选 §9.2；完整模式含 t1/t3 在 S5 验收时已绿 |
| `scripts/t1_gate.py` | PASS | S5 验收会话内绿（默认 skip_intake） |
| `scripts/t3_lg_gate.py` | PASS | 含 intake 节点拓扑后仍绿 |

开发快环：

```bat
set INTAKE_GATE_FAST=1
.venv-314\Scripts\python.exe scripts\intake_gate.py
```

---

## Claim ladder（诚实）

**可以说：**

- 题面 md/PDF 文字层 → OCR 原文 → 结构化 brief/`intake.json`
- 圆管 B 题四问清单与附件路径可审计；intake 与求解分离
- 默认图路径 **skip_intake**，不破坏 T1–T3

**不可以说 / 本 closeout 未宣称：**

- 本轮 **未** 对圆管题重新 solve（无新 objective）
- OCR 对扫描件 100% 正确（本 smoke 输入为已 OCR 的 brief 文本）
- intake 替代数模专家或保证全局最优
- Paddle 图 OCR 已生产接好（仍为 S2 占位）

---

## Runtime law (1.1 addendum)

1. Numbers still **only** from solve tools + validate.  
2. Intake artifacts **must not** carry solution-shaped keys.  
3. LG：`skip_intake=true` by default；`--intake-in` 开启前门。  
4. CLI：`orpath.bat intake` / `orpath.bat gate-intake`。

---

## Not in 1.1

- OpenPi 拖拽导入 UI  
- Live Pi 多 Agent 做 parse  
- MinerU 题面主路径  
- 竞赛全卷自动求解闭环（仍走既有 solve/paper）  
- Nightly CI 强制 live OCR 云  

---

## Next（可选）

- Paddle/MCP 真图页 smoke + 证据图  
- `human_confirm_intake` 与 resume 交互打磨  
- 将 intake brief 自动喂 research 的 prompt 模板  

**1.1 CLOSED/PASS** — 以本文件 + `docs/archive/evidence/1.1-tube-cut-intake-smoke.json` + 门禁真实输出为准。
