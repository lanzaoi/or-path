# Release v0.3.1 — 说明

**相对 v0.3.0：** 过程台可读性 + LIVE 调研超时误杀修复 + 脏产物防覆盖；不改产品主叙事。

## 为何要发 0.3.1

v0.3.0 发出后实战暴露：

| 问题 | 修复 |
|------|------|
| LIVE 默认 lead 超时 360s | `orpath_watch_run` 默认 **1800s** |
| 宽调研子任务已写完但 lead 超时 → `exit_code=-9` 整站失败 | research **timeout salvage**（产物齐全则过门禁） |
| Watch 错误条英文难懂 | `error.explain` 中文三卡：怎么了/为什么/你怎么做 + 中文 CTA |
| 无 --fresh/--resume 覆盖已有 thread stages | `dirty_artifacts` 拒绝隐式重跑（需明确 fresh/resume/force） |

## 版本号

- `VERSION` → `0.3.1`
- Tag：`v0.3.1`
- 资产：`orpath-0.3.1-win-x64.zip` + `SHA256SUMS` + `install.ps1`

## 验收（作者机）

```bat
set PYTHONPATH=
.venv-314\Scripts\python.exe scripts\dialogue_steer_gate.py
.venv-314\Scripts\python.exe -m orpath.run_orpath run --fresh --slug poly-q1-smoke --problem-id polyomino_b_q1 --problem-class polyomino_cover --solve-mode polyomino --knowledge-mode off --no-live-subagent
.venv-314\Scripts\python.exe scripts\pack_release.py
.venv-314\Scripts\python.exe scripts\l2_release_gate.py --zip dist\orpath-0.3.1-win-x64.zip
```

期望：dialogue PASS；poly solution+validate 数字闭环；L2 gate Watch face status=ok。

## 已知边界（仍在）

- intake 赛题无 domain adapter 时 solve **正确 BLOCKED**（禁止 fixture 金标冒充）
- 全题 B 多问仍需 pack/适配器路径，不是 resume 一句能出全解
- `p5_polish_gate` 若缺可选 Langfuse 文档仍可能红（非 L2 硬依赖）

## Changelog

### Fixed
- LIVE research lead 360s 误杀 → 1800s + timeout salvage
- Watch 报错中文可视化（explain + CTA）
- 隐式重跑已有 stages 的脏产物保护

### Docs
- 本说明 `docs/archive/releases/v0.3.1-notes.md`
