# Release v0.3.5 — 说明

**相对 v0.3.1：** 合并外部更新 —— 异形圆管 B2026 全题求解/校验/门禁管线、协作协议、公开 CVRPLIB 基准；同时清理本地实验记录、更新安装面文档。

## 为何要发 0.3.5

| 新增/变化 | 内容 |
|------|------|
| 圆管 B2026 全题求解 | `tools/solve_tube_cut_b2026.py` 重写；新增 `tools/tube_optimization.py`（~1850 行） |
| 校验增强 | `tools/validate_solution.py` 大幅扩展（+830 行），缺失文件结构化 JSON |
| 门禁 | `tube_live_gate` 重写；新增 redteam / geometry / collaboration gates |
| 协作协议 | `contracts/tube_collaboration_v1.json` · 实验卡 schema · templates |
| 公开基准 | `fixtures/t3/cvrplib_a_n32_k5/`（TSPLIB CVRP n=32）· `eval_or_bench` 脚本 |
| Pi 角色 | `.pi/agents/or-tube-*` 6 个新 agent 定义 |
| 文档 | tube 优化计划/协作说明/vrp 公开基准；install 版本表更新 |

## 版本号

- `VERSION` → `0.3.5`
- Tag：`v0.3.5`
- 资产：`orpath-0.3.5-win-x64.zip` + `SHA256SUMS` + `install.ps1`

## 安装

```powershell
irm https://github.com/lanzaoi/or-path/releases/download/v0.3.5/install.ps1 | iex
```

## 验收（作者机）

```bat
set PYTHONPATH=
.venv-314\Scripts\python.exe scripts\export_demo_seed.py --check
.venv-314\Scripts\python.exe scripts\pack_release.py --platform win-x64
.venv-314\Scripts\python.exe scripts\l2_release_gate.py --zip dist\orpath-0.3.5-win-x64.zip
```

## 诚实边界

- 圆管结果 = 启发式 **FEASIBLE**（Q1=100000 / Q3=99000 / Q4=260000 mm），**非** proven OPTIMAL。
- 演示 seed 回放 ≠ 此刻 LIVE；数字只认 solve + validate JSON。
