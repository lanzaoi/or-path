# T2 Portfolio talk (45–60s)

「OR-Path 是带**验证关卡**的 Supervisor–Worker 流水线：LangGraph 管阶段，Pi 子 Agent 做隔离研究/建模，**数字只来自 OR-Tools/NetworkX**，并且用 validate **重算**路径/行程/多车容量。

T2 在 T1 全链之上加厚了三类题——最短路、TSP n=8、两车 VRP——schema 禁止最优值；validate 失败会先有界调参再回修建模。

知识竖切：MinerU/语料预处理 → LightRAG 语义 stub + BM25 + FTS → RRF，Researcher 消费带 `chunk_id` 的检索制品；Cognee 只记教训不记 objective。

门禁双轨：本地 `t2_gate` 与云侧 `t2_gate_cloud`（嵌入、混合检索、在线 arXiv R1）。OpenPi 截图与真子 Agent 轨迹证明 live 隔离。」
