# TSP n=8 — OR-Path T2 fixture

Find a minimum-length tour visiting all cities exactly once and returning to the start (city 0).

Coordinates are Euclidean; solver uses rounded integer distances.

Do **not** invent the optimal length in modeling — call solve tools.
