# Process lessons (OR-Path)

Cross-run **process memory** cards (how we solved / pitfalls).  
Not authoritative optima. Pipeline auto-retrieves into `notes/<slug>-lessons.*`.

- Seed JSON in this directory is tracked.
- Local drafts: `memory/lessons/` (gitignored).
