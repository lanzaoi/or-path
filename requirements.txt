# OR-Path Python deps (T1 + T2 + exact solvers)
langgraph>=0.2
langgraph-checkpoint-sqlite>=2
pytest>=8
pydantic>=2
httpx>=0.27
networkx>=3
ortools>=9.10
highspy>=1.7
rank-bm25>=0.2.2
# LightRAG often installed as lightrag-hku; keep flexible
pi-py-sdk>=0.2
python-dotenv>=1.0
# 1.1 intake OCR — PDF text layer + onnxruntime image OCR
pypdf>=4
rapidocr-onnxruntime>=1.3.0
# Modeling API (pulled industry standard; solvers remain networkx/cpsat/highs/ortools)
pulp>=2.8
# Tube adapter + result workbook export (raw contest data remains local/ignored)
numpy>=1.26,<3
pandas>=2.2,<3
openpyxl>=3.1
# Optional VRP SOTA engine (github.com/PyVRP/PyVRP) — not default claim ladder
# pyvrp>=0.9

# --- optional OR engines (pulled; not default claim ladder) ---
# pyvrp>=0.9
# pyjobshop>=0.0.9
# ALNS>=7
# vrplib
# stockpyl  # heavy docs deps; install with --no-deps if needed
