# OR-Path Python deps (T1 + T2 + exact solvers)
langgraph>=0.2
pytest>=8
pydantic>=2
httpx>=0.27
networkx>=3
ortools>=9.10
highspy>=1.7
rank-bm25>=0.2.2
# LightRAG often installed as lightrag-hku; keep flexible
pi-py-sdk>=0.2
python-dotenv>=1.0
# 1.1 intake OCR — PDF text layer + image OCR fallback
pypdf>=4
rapidocr-onnxruntime>=1.3
opencv-python-headless>=4.8
# Optional heavy: system Python311 paddleocr/ppocr via ORPATH_PADDLEOCR_PYTHON
