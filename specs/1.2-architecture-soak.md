# 1.2 Architecture Soak — Grill Freeze

**Status:** LAW (frozen 2026-07-31; behavior change requires editing this file first)  
**Milestone:** **1.2** (after 1.1 CLOSED; does **not** reopen T1/T2/T3/1.0/1.1 DoD)  
**Grill confirm:** 2026-07-31 — user locked Q1–Q17 + fallback

---

## 1. One-liner

**1.2 is an architecture soak / contest-shell stress test**, not a new OR product class and not a “C-题交卷 PASS” milestone.

Primary probe: feed a **non-routing real contest PDF+xls** through the **product LangGraph**, with **Pi-only numerics**, **full live multi-agent** on forced stages, and **honest solve failure** when no domain adapter exists. Measure shell stability, cosplay resistance, intake, and monitor discipline under a tight wall clock.

---

## 2. Problem under test

### 2.1 Primary

| Field | Value |
|-------|--------|
| Contest | 杭州电子科技大学 2025 第七届「美珈羽杯」数学建模新生赛 |
| Problem | **C 题** 服装销售线上线下协同发展背景下的数据赋能 |
| Host path (first try) | `C:\Users\Lanzao\Desktop\水赛\2025杭州电子科技大学第七届”美珈羽杯“数学建模新生赛赛题\C题 服装销售线上线下协同发展背景下的数据赋能\` |
| Surface | `C题 服装销售线上线下协同发展背景下的数据赋能.pdf` |
| Data | `附 C题-优衣库数据.xls` (~4.5MB) |
| Subproblems | Q1 顾客群体↔消费行为 · Q2 国庆线上/线下差异显著性 · Q3 门店布局重要性（3 线上+3 线下） · Q4 利润因素 + **未来 2 周利润预测表** + 管理启示 |
| Domain vs OR-Path core | **Stats / segmentation / ranking / forecast** — **not** SP/TSP/VRP/packing |

### 2.2 Fallback (if primary “不行”)

| Field | Value |
|-------|--------|
| Problem | **圆管 B2026**（异形圆管下料 / tube-cut） |
| When | See §6 failure triggers |
| Solve policy on fallback | **D — also do not attach existing solvers for scoring**; still soak shell / FAIL / MA / monitor (contrast with C). **Not** “C failed → full tube E2E optima demo” unless a **new** freeze says so. |

### 2.3 Data placement

1. **First:** read external absolute paths under `Desktop\水赛\…`  
2. **If read fails** (encoding, permissions, xls deps, etc.): copy into `fixtures/intake/…` (B path) with large binaries gitignored as needed  

---

## 3. Freeze table (authoritative)

| ID | Lock |
|----|------|
| **Q1** DoD shape (intent) | Originally E2E + live MA hard; **closed by Q9** to architecture evidence pack |
| **Q2/Q3** Problem | HDU C 2025 retail (primary); tube B fallback |
| **Q4** Product claim | Contest assist / architecture soak — **no** new “retail OR engine” product class |
| **Q5** Pressure direction | Submission-complete was aspirational; **acceptance = Q9** |
| **Q6** Parent boundary | **Pi-only hard**: Hermes = OCR, paths, launch, **full monitor**, closing report. **No** analysis scripts, **no** rewriting Q tables, **no** alternate full solve presented as Pi’s |
| **Q7** Topology | **Product LG full graph** (`intake` → … → solve → validate → paper…) |
| **Q8** Solve (primary C) | No C-domain adapter → **honest hard FAIL** (no mock pass, no skip-solve success) |
| **Q9** PASS definition | See §4 |
| **Q10** Live MA | **All forced live:** research + model + cite_pack + review_pack (`subagent` toolCall + child transcripts; anti-cosplay harness) |
| **Q11** After solve FAIL | **Still run paper shell**; draft may use intake/research/model only; cite/review **still live**; R2/claim/provenance **BLOCKED or explicit skip**; **forged Q4 forecast table = red flag** |
| **Q12** Time box | Wall clock **≤45–60 min** total; per-lead **≤12–15 min**; timeout = legitimate stop + report |
| **Q13** `human_confirm_intake` | **Off** |
| **Q14** Data | External first → fixtures if needed |
| **Q15** `problem_id` | **Borrow shell** (e.g. `shortest_path`) so the graph runs; **true problem = intake only**. **Forbidden:** treating fixture gold **42 / 45 / 58** as C-题 answers (monitor red flag) |
| **Q16** Knowledge | Follow **`run_orpath` default** (do not special-case off/hybrid for this soak unless ops requires) |
| **Fallback solve** | Tube B: **D** — do not attach existing tube solvers for this soak leg |

---

## 4. PASS / FAIL (1.2 this milestone)

### 4.1 May claim PASS (architecture evidence pack)

- [ ] Intake artifacts + `gate_intake` (or equivalent) green for the chosen surface  
- [ ] Product graph runs with intake on; reaches **solve** and records **honest FAIL** (or equivalent hard stop) **without** mock gold solution for C  
- [ ] Failure semantics / artifacts are inspectable (state, logs, no silent success)  
- [ ] Live MA evidence on **research, model, cite, review**: real `toolCall`/`tool_execution_*` with `subagent` + non-empty child transcripts under install home  
- [ ] Paper stage either BLOCKED/skip on R2/claim **or** draft without forged forecast tables  
- [ ] Hermes monitor report on disk (paths, mtimes, EXIT, red flags) — numbers cited **only from disk**  
- [ ] Closeout claim ladder honest (§5)  
- [ ] No secrets / full contest PII dumps forced into git  

### 4.2 Must NOT claim as 1.2 PASS

- C-题 Q1–Q4 fully solved with trusted forecast table  
- Product now “supports retail data-empowerment OR”  
- Parent-solved tables labeled as Pi  
- Mock/`shortest_path` gold as C success  
- Bare `pi -p` role labels without subagent trajectories  
- Tube fallback “we got optima” when freeze says solve-detached soak  

### 4.3 Expected stress (not automatic fail of the *milestone* if handled honestly)

- Solve hard-FAIL on C (by design)  
- Wall-clock stop at 45–60 min  
- Cosplay retries exhausted → stop + report  
- Operator stop on red flags (authorized)

---

## 5. Claim ladder

| May say | Must not say |
|---------|----------------|
| We soaked product LG + intake + full live MA on a non-routing contest PDF | We delivered a complete C-题 award paper via OR-Path solvers |
| Solve failed closed-loop because no retail adapter (intentional law) | SP objective 42 is the clothing answer |
| Paper provenance BLOCKED without solution numerics | R2 passed on invented 2-week profits |
| Fallback tube B also soaked shell without attaching pack solvers | 1.2 = tube E2E optima closeout |

---

## 6. Fallback triggers (“不行” → consider tube B)

Any of:

1. External PDF/xls cannot be ingested after reasonable path/encoding/dep attempts (including fixtures B path still failing intake)  
2. Within time box: **zero** real subagent toolCalls on forced stages (persistent cosplay / env break)  
3. Borrowed shell **mislabels fixture gold as C success** and cannot be forced into honest FAIL  
4. Graph dies before intake/orchestrate with no actionable monitor close  
5. User verbal: “这题没测到架构”

On fallback: **re-confirm launcher slug/paths**; inherit Pi-only, full live MA, tight time box, architecture-pack PASS; **solve remains detached (D)**.

---

## 7. Parent (Hermes) monitor law

Aligned with Pi-only contest assist:

| MAY | MUST NOT |
|-----|----------|
| OCR, path brief, launch product/Pi leads, poll mtime/EXIT/json | Write models, packing, forecasts, or “better” Q tables |
| Stop on red flags / timeout | Patch objectives mid-run silently |
| Write `*_MONITOR.md` from **disk only** | Claim multi-agent without transcripts + detect |

Red flags include: cosplay exhausted, timeout, gold misbind (42/45/58), forged Q4 table, runaway disk, hung lead past per-lead cap.

---

## 8. Relation to other specs

| Spec | Relation |
|------|----------|
| `problem-intake.md` | Intake contracts still bind; no solution keys in intake |
| `control-plane.md` / `t3-lg-skeleton.md` | Product graph topology; 1.2 does not fork a second boss |
| `multi-agent.md` / anti-cosplay | Live stages use harness; bare `-p` ≠ MA |
| `solvers-and-validate.md` | C has no adapter → FAIL; do not weaken global “numbers from solve+validate” for OR classes |
| `gates-and-dod.md` | Pointer section for 1.2; existing gates stay green (regression) |
| `product-scope.md` | 1.2 soak does **not** expand advertised problem classes |

**Regression:** `t1_gate` / `t3_lg_gate` / `intake_gate` remain green with default `skip_intake` where applicable. 1.2 live soak is **not** required inside those CI gates (`ORPATH_LIVE_SUBAGENT=0` stays for deterministic gates).

---

## 9. Out of scope (1.2)

- Formal retail/stats product class + production `solve_retail_*` (unless future milestone)  
- Guaranteed C-题 submission score  
- OpenPi GUI import polish as hard DoD  
- Compose/K8s  
- Homegrown IR eval sets  
- Reopening closed ADR-0001…0006 DoD  
- Tube full optima demo under this freeze’s fallback D  

---

## 10. Implementation order (when user says 开工)

1. Path smoke: PDF text/OCR + xls readable?  
2. Intake CLI on C surface (`human_confirm` off)  
3. `run_orpath` with intake-in, borrowed `problem_id`, live subagent on, time caps  
4. Monitor through solve FAIL → paper BLOCKED path  
5. Evidence pack + short closeout  
6. Only if §6: tube B soak with solve detached  

**No implementation in grill phase** — this file is freeze only until explicit start.

---

## 11. Doc pointers

- Smoke/ops (when written): `docs/1.2-smoke.md`  
- Closeout (when done): `docs/1.2-closeout.md`  
- Evidence: `docs/archive/evidence/`  
- External trees: do not bulk-commit `Desktop\水赛` binaries  

**1.2 freeze confirmed** — 2026-07-31.

---

## 12. Residual R1–R4 (landed 2026-07-31)

After primary soak, user approved residual fixes:

| ID | Law |
|----|-----|
| R1 | BLOCKED solution → revise short-circuit to provenance (no multi-round cite/review burn) |
| R2 | intake-on research/model briefs: intake brief/json **primary**; fixture is `shell_only` |
| R3 | Launchers: `PYTHONUNBUFFERED=1` + `python -u`; monitors prefer `runs/*/stages` + `outputs/.agents` over log growth |
| R4 | BLOCKED/intake paper whitelist = intake-scoped JSON (no shell fixture arxiv); draft `fixture: shell_only:…` |

Verify residual: slug `hdu-c2025-1.2-r` (LIVE=0) shows single cite+review then revise `blocked` → provenance.
